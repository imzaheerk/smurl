/**
 * Inversify binding for Custom Domain feature.
 * Add container bindings here when using DI.
 */
export function createCustomDomainBinding(/* container: Container */): void {
  // No-op: use direct imports until Inversify is adopted app-wide.
}
