/**
 * Inversify binding for URL feature.
 * Add inversify container bindings here when using DI.
 * Example:
 *   container.bind<UrlRepository>(TYPES.UrlRepository).to(UrlRepository);
 *   container.bind<UrlUsecase>(TYPES.UrlUsecase).to(UrlUsecase);
 */
export function createUrlBinding(/* container: Container */): void {
  // No-op: use direct imports until Inversify is adopted app-wide.
}
