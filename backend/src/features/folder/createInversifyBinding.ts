/**
 * Inversify binding for Folder feature.
 * Add container bindings here when using DI.
 */
export function createFolderBinding(/* container: Container */): void {
  // No-op: use direct imports until Inversify is adopted app-wide.
}
